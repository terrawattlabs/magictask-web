angular.module('zapexApp')
  .controller('SmokeCtrl', function ($scope, $routeParams, $location, $http, $stamplay, $cookies, LoginService) {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];



$scope.service = $location.search();




  });
